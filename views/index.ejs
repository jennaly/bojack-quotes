<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Bojack Horseman Quotes</title>
    <link rel="stylesheet" href="/styles.css"/>
</head>
<body>
    
    <h1 data-position="header">Bojack Horseman Quotes</h1>

    <section data-position="add-quote">
        <h2>Add quote</h2>
        <form action="/quotes" method="POST">
            <input type="text" placeholder="name" name="name">
            <input type="text" placeholder="quote" name="quote">
            <button type="submit">Submit</button>
        </form>
    </section>

    <section data-position="update-quote">
        <div>
            <h2>Bojack takes over</h2>
            <p>
                Replace the first Sara Lynn quote with one by Bojack. 
            </p>
            <button id="update-button">Replace Sara Lynn's quote</button>
        </div>
    </section>

    <section data-position="delete-quote">
        <div>
            <h2>Remove Bojack!</h2>
            <p>Delete one Bojack Horseman's quote. Does nothing if there are no more Bojack quotes.</p>
            <button id="delete-button">Delete Bojack Horseman's quote</button>
        </div>
        <div id="message"></div>
    </section>

    <section data-position="quotes">
        <h2>Quotes</h2>

        <ul class="quotes">
            <!--loop through quotes-->
            <% for (var i = 0; i < quotes.length; i++) { %>
                <li class="quotes">
                    <span><%= quotes[i].name %></span>
                    <span><%= quotes[i].quote %></span>
                </li>
            <% } %>
        </ul>
    </section>

<script src="/main.js"></script>
</body>
</html>